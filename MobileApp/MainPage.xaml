<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MobileApp.MainPage">

    <ScrollView>
		<VerticalStackLayout Padding="20">
			<Label Text="Zamówienia" FontSize="24"/>
			<Button Text="Odśwież" Clicked="OnRefreshClicked"/>

			<CollectionView x:Name="OrdersList">
				<CollectionView.ItemTemplate>
					<DataTemplate>
						<Border Stroke="Gray" Padding="10" Margin="5">
							<VerticalStackLayout>

								<Label Text="{Binding Id, StringFormat='Order ID: {0}'}" FontAttributes="Bold"/>

								<Label Text="{Binding Status, StringFormat='Order status: {0}'}" />

								<Label Text="{Binding CreatedAt, StringFormat='Date of order: {0}'}" FontSize="12" />

								<CollectionView ItemsSource="{Binding Items}">
									<CollectionView.ItemTemplate>
										<DataTemplate>
											<HorizontalStackLayout Spacing="10">
												<Label Text="{Binding Quantity, StringFormat='x{0}'}"/>
												<Label Text="{Binding Name}"/>
											</HorizontalStackLayout>
										</DataTemplate>
									</CollectionView.ItemTemplate>
								</CollectionView>
							</VerticalStackLayout>
						</Border>
					</DataTemplate>
				</CollectionView.ItemTemplate>
			</CollectionView>
		</VerticalStackLayout>
    </ScrollView>

</ContentPage>
